# : Manager [[[

[manager]
cwd = { fg = "#e5e1e9" }

# Tab
tab_active = { fg = "#2d2960", bg = "#c4c0ff", bold = true }
tab_inactive = { fg = "#e3dfff", bg = "#17134a" }
tab_width = 1

# Find
find_keyword = { fg = "#ffb4ab", bold = true, italic = true, underline = true }
find_position = { fg = "#ffb4ab", bold = true, italic = true }

# Marker
marker_copied = { fg = "#ff72af", bg = "#ff72af" }
marker_cut = { fg = "#ffd8e9", bg = "#ffd8e9" }
marker_marked = { fg = "#ffb4ab", bg = "#ffb4ab" }
marker_selected = { fg = "#ebb9d0", bg = "#ebb9d0" }

# Count
count_copied = { fg = "#2f1123", bg = "#ffd8e9" }
count_cut = { fg = "#2f1123", bg = "#ffd8e9" }
count_selected = { fg = "#2d2960", bg = "#ebb9d0" }

# Border
border_symbol = "│"
border_style  = { fg = "#c4c0ff" }

# : ]]]


# : Status [[[

[status]
separator_open = "🭁"
separator_close = "🭠"
separator_style = { bg = "#2d2960", fg = "#F4A261" }

[mode]
# Mode
normal_main = { bg = "#c4c0ff", fg = "#2d2960", bold = true }
normal_alt  = { bg = "#47464f", fg = "#c8c5d0" }

# Select mode
select_main = { bg = "#c7c4dc", fg = "#302e42", bold = true }
select_alt  = { bg = "#47464f", fg = "#c8c5d0" }

# Unset mode
unset_main = { bg = "#ebb9d0", fg = "#472638", bold = true }
unset_alt  = { bg = "#47464f", fg = "#c8c5d0" }

# Progress
progress_label = { bold = true }
progress_normal = { fg = "#c4c0ff", bg = "#3a383f" }
progress_error = { fg = "#ffb4ab", bg = "#3a383f" }

# Permissions
permissions_t = { fg = "#6c64a3" }
permissions_w = { fg = "#c94280" }
permissions_x = { fg = "#ff2b12" }
permissions_r = { fg = "#ff3f93" }
permissions_s = { fg = "#5d46ff" }

# : ]]]


# : Select [[[

[select]
border = { fg = "#c4c0ff" }
active = { fg = "#ebb9d0", bold = true }
inactive = {}

# : ]]]


# : Input [[[

[input]
border = { fg = "#c4c0ff" }
value = { fg = "#e5e1e9" }

# : ]]]


# : Completion [[[

[completion]
border = { fg = "#c4c0ff", bg = "#2d2960" }

# : ]]]


# : Tasks [[[

[tasks]
border = { fg = "#c4c0ff" }
title = {}
hovered = { fg = "#ffd8e9", underline = true }

# : ]]]


# : Which [[[

[which]
cols = 3
mask = { bg = "#3a383f" }
cand = { fg = "#c4c0ff" }
rest = { fg = "#2d2960" }
desc = { fg = "#e5e1e9" }
separator = " ▶ "
separator_style = { fg = "#e5e1e9" }

# : ]]]


# : Help [[[

[help]
on = { fg = "#e5e1e9" }
run = { fg = "#e5e1e9" }
footer = { fg = "#302e42", bg = "#c7c4dc" }

# : ]]]


# : Notify [[[

[notify]
title_info = { fg = "#ebb9d0" }
title_warn = { fg = "#c4c0ff" }
title_error = { fg = "#ffb4ab" }

# : ]]]


# : File-specific styles [[[

[filetype]

rules = [
    # Images
    { mime = "image/*", fg = "#94e2d5" },

    # Media
    { mime = "{audio,video}/*", fg = "#f9e2af" },

    # Archives
    { mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#f5c2e7" },

    # Documents
    { mime = "application/{pdf,doc,rtf}", fg = "#a6e3a1" },

    # Special files
    { name = "*", is = "orphan", bg = "#93000a" },
    { name = "*", is = "exec", fg = "#ffdad6" },

    # Fallback
    { name = "*", fg = "#e5e1e9" },
    { name = "*/", fg = "#c4c0ff" },
]

# : ]]]
